# 2026-07-05 工作日志

## factor_timing 策略优化（v2.3.0）

### 修改内容
1. **接入真实数据源**：
   - `far_close`：通过 `MultiSourceAdapter.get_term_structure()` 获取真实远月价格（TDX→东方财富自动降级）
   - `warehouse_receipt`：通过 `DuckDBStore.get_latest_warehouse()` 获取真实仓单数据（目前 DuckDB 数据空，降级到估值）

2. **关闭板块中性标准化**：`sector_neutral: false`，改为全市场跨品种标准化

3. **OI 三角过滤 + ADX 条件**：仅当 `ADX > 25` 时启用 OI 三角调整

4. **多因子投票 + G1/G10 截断**：
   - 每个因子独立投票（Z>0.1 投 bull，Z<-0.1 投 bear，否则弃权）
   - 过半数一致（置信度≥51%）赋予全分，否则打折
   - G1（Top3）：动手组，正常信号
   - G10（Bottom3）：观望组，分数≤30，标记 WATCH

5. **降频预留**：`kline_period` 配置参数已添加，当前为 `daily`，待 scan_all.py 层支持传入 4h/1h K 线

## v2.3.1 升级（2026-07-05 第二次迭代）

基于掌柜12点代码审查，批量优化 factor_timing.py：

### P0 数据质量
1. far_close 降级改用主力-次主力价差（从 df_map 取波动率推算）
2. wr_last_year 降级使用 60 日滚动 OI 均值

### P1 核心算法
3. 板块中性：改为全局Z→减板块均值（保留方差）
4. OI过滤：增加全市场ADX二级闸门
5. 投票改为十分组法：qcut十分组→统计头部/尾部票数→过半才出手
6. 换月检测：基于OI变化（5日均值骤降50%触发）

### P2 参数细节
7. 动量复权处理说明（依赖上游复权数据）
8. 偏度改为30d+60d等权合成（替代纯60d）
9. 量价增加OI修正版：corr(ΔP, ΔOI) 20日
10. veto增加：OI异常>20%扣5、涨跌停≥5%扣10、板块分歧扣3
11. 输出共振系数（四子层方向一致数）
12. 市场状态自适应：全市场ADX+波动率检测→trending/choppy/high_vol/low_vol→自动切换参数

### 测试结果
- 自动检测为 trending 市，strong_threshold 从75降至70
- 新字段全部输出：vote_net, vote_confidence, resonance, market_state, g_group, ts_type

## scan_all.py 双策略模式（--dual）

新增 `--dual` 模式：
- 内部递归调用 `run_scan()` 两次，分别跑 `layered_l1l4` 和 `factor_timing`
- 输出文件自动加 `_l1l4` / `_factor_timing` 后缀
- L1-L4 → 技术分析师 Agent
- factor_timing → 基本面分析师 Agent
- **新增辩论共识报告 (consensus)**：百分位对齐 + 3x3矩阵裁决
  - 信号来源：`signals/debate_consensus.py`
  - 解决口径不统一问题（L1-L4 ±100 vs factor_timing ±15）
  - 自动生成 `_consensus_{date}.json` 和 `.html`
  - 裁决理由: consensus / l1l4_primary / factor_primary / percentile_higher / skip
- SKILL.md 默认推荐命令改为 `python scripts/scan_all.py --dual`

### 端到端测试结果
- 语法检查：通过
- 策略加载：通过（`factor_timing: 因子择时(v2.3,投票+G1/G10)`）
- 期数据获取：4/4 品种真实期限结构成功（CU/RB/PK/P）
- 仓单获取：0/4（DuckDB warehouse 表空，降级到估值）
- 信号输出正常，包含新字段：`vote_net`, `vote_confidence`, `g_group`, `ts_type`

### 已知限制
- DuckDB warehouse 表暂无实时填充机制，需要额外实现仓单数据采集

### 备份
- 原版保存为：`strategies/factor_timing.py.bak.20260705`

## 自演进机制（2026-07-05 第三次迭代）

在 `signals/debate_consensus.py` 中内置自演进监控：

**触发条件**（需同时满足）：
1. 冲突率 > 20%
2. 冲突中因子平均 abs ≥ 55  
3. 因子强度 > L1-L4 强度

**演进时序**：
- monitor（连续 <21天满足条件）
- propose（连续 22-29天满足条件，每天输出演进提案）
- evolved（连续 ≥30天且用户未干预 → Expert自动切换到方案B）

**用户干预**：修改 `evolution_state.json` 中 `user_override` 为 `"stay_A"` 或 `"go_B"`

**状态持久化**：`scripts/.workbuddy/memory/evolution_state.json`

**当前状态**：条件不满足（因子强度3.5 << L1-L4强度58.5），演进不会触发
