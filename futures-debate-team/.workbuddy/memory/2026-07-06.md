# 2026-07-06 — /loop Bridgewater方法论CTA落地全量实施

## 实施范围

基于 `docs/Bridgewater方法论_CTA落地_完整实施手册.md` 全量实施：

### Phase 1: 辩论基础增强 (Path C, P0)
- `debate_brief.py`: 新增 `compute_debate_score()` 五维加权评分 + `build_judge_brief()` 闫判官速览
- `debate_history.py`: 新增轻量JSON档案（append-only），含 `load_feedback/record_feedback/get_symbol_value_score`
- `select_debate_symbols()` 增强：候选品种字段从4个→12个，排序从简单divergence_score→五维debate_value

### Phase 2: 研报质量过滤 (Path B, P1)
- `quality_filter.py`: B-Minimal规则评分 + B-Standard自动标注框架
- 覆盖五维checklist：量化/供给/需求/方向/时效

### Phase 3: ML训练自动化 (Path C-Phase 4 + ML框架)
- `auto_train_orchestrator.py`: TrainingOrchestrator完整实现（LightGBM/XGBoost/Sklearn）
- DisputePredictor：争议度预测指标提取+LightGBM训练

### 测试覆盖
- 4个测试文件 + 1个覆盖率补充文件，共70个测试
- quality_filter.py: 96%, debate_history.py: 90%, debate_brief.py: 75%, auto_train_orchestrator.py: 55%

## 核心修复
- `debate_history.py`: 模块级常量 → 函数级动态env var读取（支持测试运行时切换目录）
- `auto_train_orchestrator.py`: 修复timezone naive/aware datetime比较 + 首次训练条件逻辑
- `quality_filter.py`: 需求模式列表补充"需求"关键词

## 测试文件统一迁移（2026-07-06）

将原来分散在7个skill目录下的测试文件统一迁移到项目根 `tests/` 目录。

### 新结构
```
futures-debate-team/
├── tests/                          ← 统一目录
│   ├── quant-daily/                ← 5个测试文件（我们的）
│   ├── commodity-chain/            ← 6个旧测试文件
│   ├── debate-argument-builder/    ← 1个
│   ├── debate-risk-manager/        ← 1个
│   ├── fundamental-data-collector/ ← 1个
│   ├── technical-analysis/         ← 1个
│   └── contracts/                  ← 1个
├── run_all_tests.py                ← 分批运行所有测试
```

### 注意事项
- 不同 skill 的 conftest.py 会修改全局 sys.path，不能同时运行
- `pytest` 默认仅运行 `tests/quant-daily/`
- 运行所有测试：`python run_all_tests.py`
- 旧 `skills/*/tests/` 目录已清理

### 自动化体系（2026-07-06）

### 自动流水线
| 组件 | 说明 | 调度 |
|:-----|:-----|:-----|
| `run_auto_pipeline.py` | 全自动数据管道包装器（scan→chain→debate_brief→assemble→report→history→ML） | 工作日 16:15 |
| 自动化「全自动辩论专家团流水线」 | 包装器 + Agent辩论 + 历史记录 + 报表生成 | 工作日 16:15 CST |
| 自动化「ML模型自动训练检查」 | TrainingOrchestrator 每周ML训练条件检查 | 周日 17:00 |

### 零人工干预设计
- 数据管道: run_auto_pipeline.py 自动串联所有步骤，失败步骤自动跳过不阻断
- Agent辩论: 明鉴秋自动协调全流程（数技源→链证源→闫判官→证真/慎思→风控明→策执远）
- 历史记录: debate_history 自动 append-only 记录每次辩论
- ML训练: TrainingOrchestrator 自主决策训练时机并自动部署

### 文件变更
| 文件 | 操作 | 路径 |
|:-----|:-----|:-----|
| debate_brief.py | 修改 + 新增函数 | skills/quant-daily/scripts/signals/ |
| debate_history.py | 新增 | skills/quant-daily/scripts/signals/ |
| quality_filter.py | 新增 | skills/quant-daily/scripts/signals/ |
| auto_train_orchestrator.py | 新增 | skills/quant-daily/scripts/signals/ |
| run_auto_pipeline.py | 新增 | skills/quant-daily/scripts/ |
| unified_logger.py | 新增（测试桩） | skills/quant-daily/scripts/ |
| pyproject.toml | 修改(测试路径) | 根目录 |

## 文档更新与GitHub推送（2026-07-06）

- README.md 升级到 v4.5：新增 Bridgewater 方法论、系统架构图、五维评分表、自动流水线说明
- pyproject.toml 版本号 4.4.0 → 4.5.0
- 同步到 CTAAgents/experts (commit c7b45ee)
- 清理 coverage 产物文件 (*.py,cover)

## 自动发布体系搭建（2026-07-06）

创建 scripts/auto_publish.py 自动完成：版本号自增 + README changelog 追加 + GitHub推送。
更新2个自动化：
- 「全自动辩论专家团流水线」Phase 4 加入自动发布
- 「自动发布流水线」兜底机制（23:05）
验证通过：v4.5.0→v4.5.1 自动推送成功 (commit 6b5ebdff)

## 10:00 — TqSdk盘中数据源集成（quant-daily v2.5.0）
- `multi_source_adapter.py`：新增 `_fetch_tqsdk_kline()`（TqSdk主力连续合约K线）；`get_kline()` 盘中新增TqSdk降级路径：TDX→TqSdk(盘中)→东方财富→AKShare
- `SKILL.md`：新增"数据源获取管道"章节；版本号 v2.4.0→v2.5.0
- expert-manager：validate + register 通过

## 10:08 — 分歧品种入池增加信号强度门槛
- `skills/quant-daily/scripts/signals/debate_brief.py`：分歧品种第一轮入池新增 `MIN_DIVERGENCE_SIGNAL = 30` 硬性门槛 — 两边最强信号的 |total| < 30 的弱分歧被跳过，并打印跳过的数量。避免"为辩论而辩论"，确保进入候选池的分歧至少有一边有 MODERATE 以上信号强度
- 29/29 测试通过

## 10:14 — 双通道分离：交易推荐 vs 辩论选题
- `skills/quant-daily/scripts/signals/debate_brief.py`：`select_debate_symbols()` 新增双通道分离逻辑
  - **交易推荐通道 (STRONG_RECOMMEND)**：共识方向 + launch阶段 + RSI 30-70 + |Z|<2.0 + 信号>=MODERATE → 直接推荐，跳过辩论
  - **观察级通道 (WATCH)**：共识 + (launch或非极端)至少其一 → 可关注，也可辩论
  - 符合以上条件的品种从共识列表中**移除**，不进入辩论候选池

## 10:19 — 极端分歧品种标记左侧反转机会
- `skills/quant-daily/scripts/signals/debate_brief.py`：逐品种分类循环新增极端分歧检测
  - `left_side_reversal: True` + 标签"左侧反转机会" — 当 RSI>75/<25 或 |Z|>2.5 且两策略方向相反时触发
  - `reversal_direction` — 逆转方向字段（超买→空头, 超卖→多头）
  - 分歧第一轮中，极端分歧品种的 `proposition_side` 改用逆转方向（而非信号更强的策略方向）
  - 原因字段改为"左侧反转机会: 极端XX位置+分歧, 正方=逆转方向(XX)"
  - 控制台打印极端分歧品种列表
- 100/100 全量测试通过
  - 新增 `trading_recommendations` 和 `watch_list` 作为独立输出字段

## 11:08 — 交易推荐架构确认
- 直接推荐通道的输出定位：**裸信号 + 原因**（品种/方向/强度/ADX/RSI/stage），不含入场价/止损/仓位
- 闫判官 Agent 负责查看 direct recommendations 后手动补充交易参数（入场区间/止损距/目标）
- 策执远 Agent 负责根据闫判官给的参数计算具体仓位和执行方案
- Python 管道不越界做入场/止损/仓位计算，保持职责边界清晰
- 70/70 全量测试通过

## 11:09 — 双通道 Agent MD 更新（全量适配）

### 闫判官 (`agents/futures-judge.md`)
- 准备期新增：读取 `signal_summary_candidates.json` 的 `trading_recommendations / watch_list / debate_candidates`
- 新增步骤④：**处理直接推荐** — 逐品种复核方向，基于 price/ATR/ADX 手动设定入场区间/止损距/目标价
- 新增步骤⑤：**处理观察级品种** — 决定是直接关注还是加入辩论池
- 新增"直接推荐参数设定指引"章节（含 ATR 止损/入场/目标计算公式表）
- 协作表新增策执远双路径描述；边界新增"为直接推荐品种设定交易参数"

### 策执远 (`agents/futures-trading-strategist.md`)
- Role 新增"直接推荐路径"说明
- 新增完整"直接推荐路径"履职链路（10步，含参数校验+仓位计算环节）
- Constraints 新增"可微调闫判官入场区间 ±0.1×ATR"
- Memory 记录新增 `source_path` 字段标注来源

### 明鉴秋 (`agents/futures-debate-team-team-lead.md`)
- 阶段一：产出新增 `signal_summary_candidates.json`（含双通道分离结果）
- 阶段二：拆分为"通道A直接推荐"和"通道B辩论"
- 阶段四：双通道合并流程图（辩论+直接推荐并行）
- 阶段五：合并决策表（含直接推荐 `skip` 选项）
- 新增接口3B：`direct_recommend_to_strategist` 消息协议
- 汇总输出新增双通道合并说明

### assemble_intermediate_data.py
- `build_debate_results()` 新增 `candidates` 参数
- 新增 `load_candidates()` 函数
- `--candidates` CLI 参数支持传入候选人JSON
- 中间数据透传 `trading_recommendations` / `watch_list`
- 每个品种的 `source_path` 区分 direct_recommend / watch / debate
- 100/100 全量测试通过

## 11:19 — 版本发布 v4.6.0 → v4.6.1
- `pyproject.toml`: 4.5.5→4.6.0; `plugin.json`: 4.4.0→4.5.0
- `README.md`/`USER_MANUAL.md`: 版本+架构图+核心原则同步更新
- `docs/business_flow.md`: 新规范文档(五阶段/角色对照/决策树/CLI链路)
- `skills/quant-daily/scripts/assemble_intermediate_data.py`: --candidates参数+透传
- GitHub 推送成功 (commit e4ba059e)
- expert-manager: validate + register 通过
