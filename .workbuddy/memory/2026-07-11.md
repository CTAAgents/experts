# 2026-07-11 工作日志

## 21:56 品种知识库老化维护（自动化任务 automation-1783726505869）
- 执行 `scripts/extract_knowledge.py decay`：扫描 memory/knowledge/ 下 8 品种 (au/i/jd/rb/rm/sc/ta/zn) 共 8 条 active 模式
- 结果：decayed=0, deprecated=0（全部模式为当日新建调用，距末次使用0天<60天阈值；无连续失败记录）
- 生产文件修改前已全量备份至 AppData/Local/Temp/fdt_knowledge_backup_20260711_215648/
- 产出报告：`reports/knowledge_decay_2026-07-11.md`
- 提示：consecutive_failures 字段目前无写入来源，规则B(连续3次失败→deprecated)实际不生效，建议后续评审萃取逻辑

## 22:52 修复 consecutive_failures 隐患（#2 "修隐患"）
- **问题**：`consecutive_failures` 字段无任何写入来源，规则B（连续3次失败→deprecated）形同虚设
- **修复方案**（三处编辑，`scripts/extract_knowledge.py`）：
  1. 新建模式模板添加 `"consecutive_failures": 0`（line 253）
  2. 验证通过（命中相似模式）时重置 `consecutive_failures = 0`（line 230）
  3. 新增 `record_pattern_failure(variety, pattern_name)` 公开方法 + CLI `fail` 子命令
- **集成验证**：
  - `test` 命令通过，新建模式正确携带 `consecutive_failures: 0`
  - `fail rb "多头趋势驱动型"` → `success: true`，字段正确累加
  - 连续3次 fail → 自动 deprecated，原因"连续3次验证失败"
- **触发方式**：`python scripts/extract_knowledge.py fail <品种> <模式名>`
- 备份：`AppData/Local/Temp/fdt_extract_knowledge_backup_20260711_221347.py`
